import { Theme } from '@emotion/react';

const layoutComponent = {
  header_height: 68,
  footer_height: 94,
};

const color = {
  primary: '#3182f6',
};

const theme: Theme = {
  layoutComponent,
  color,
};

export default theme;
